import { StyleSheet } from "react-native";


export const stylesSignIn = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'red',
    alignItems: 'center',
    justifyContent: 'center',
  },
  textInput:{
    width:200,
    height:50,
    borderWidth:2
  }
});
